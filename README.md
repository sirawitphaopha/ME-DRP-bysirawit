# รายงานความคลาดเคลื่อน · Med Error & DRP (v0.9.12.1)

แอปบันทึก **Med Error** และ **DRP (Drug-Related Problems)** ของห้องยา OPD + Dashboard สรุปผล
กรอกเร็ว ใช้ได้ทั้งมือถือและเว็บ เชื่อมต่อ **Supabase** และ deploy บน **Cloudflare Workers**

> โปรเจกต์นี้พัฒนาต่อจากดีไซน์ที่ทำใน Claude Design (ไฟล์ต้นฉบับอยู่ใน `project/`, บทสนทนาออกแบบอยู่ใน `chats/`)

---

## 🧱 Tech stack

| ส่วน | เทคโนโลยี |
|---|---|
| Framework | **Next.js 15** (App Router) + React 19 + TypeScript |
| ฐานข้อมูล | **Supabase** (Postgres + REST) — โปรเจกต์ `tb-calculator` ตาราง `incidents` |
| Deploy | **Cloudflare Workers** ผ่าน **OpenNext** (`@opennextjs/cloudflare`) |
| ฟอนต์ | Sarabun (ไทยมีหัว) ผ่าน `next/font` |
| ธีม | teal (หลัก) + amber (ตัวเน้น) |

**ทำไม Next.js + Cloudflare:** เป็นเครื่องมือใช้ในองค์กร (โรงพยาบาล) และมีข้อมูลผู้ป่วย (HN) —
Next.js เก็บค่า secret ฝั่ง server ได้ รองรับการต่อยอด login/สิทธิ์ผู้ใช้ (Supabase Auth) ในอนาคต
และ deploy บน Cloudflare Workers ได้ทั่วโลก (edge) ต้นทุนต่ำ

---

## ✨ ฟีเจอร์

**หน้ากรอกข้อมูล** (สลับ Med Error / DRP)
- วันที่ default เป็นวันนี้ (แก้ได้) + ป้าย "วันนี้" · **เวลาที่พบ = ปุ่มเลือกเวร** (เวรเช้า/บ่าย/ดึก · เลือกอัตโนมัติตามเวลาปัจจุบัน 08–16/16–24/00–08 · กดสลับเองได้) + นาฬิกาเดินจริง
- HN แป้นตัวเลขล้วน (inputmode numeric)
- ประเภท Error เป็นชิปกดเลย + คำอธิบายภาษาไทยเมื่อเลือก
- **ลักษณะความคลาดเคลื่อน** เลือกได้หลายอัน + คำอธิบายแบบยาวของทุกอันที่เลือก ("อื่น ๆ" มีช่องระบุ)
- **NCC MERP A–I** จัดเป็น 4 โซนมีกรอบ + สีตามความรุนแรง (เทา/เขียว/ส้ม/แดง) + ปุ่มดูความหมายครบทุกระดับ
- **ชื่อยาหลายตัว** (+ เพิ่มยา / ลบทีละแถว)
- DRP: ประเภทปัญหา + สาเหตุ + intervention (dropdown) + ผลลัพธ์ (chip)
- ธง High-alert / LASA · แนบรูป (ย่อขนาดอัตโนมัติ เก็บเป็น data URL)
- **ตรวจฟอร์มก่อนบันทึก** ไฮไลต์ช่องจำเป็นสีแดง · **บันทึกร่างอัตโนมัติ** (กู้คืนเมื่อกลับมา)
- **ผู้รายงาน** = เมนูเลือกจากรายชื่อเจ้าหน้าที่ห้องยา OPD 16 คน (v0.9.1 · กันพิมพ์ผิด)

**หน้ารายการ** (แยกจาก Dashboard)
- กรองได้ทุกฟิลด์ (ประเภท/จุดที่พบ/Error/ระดับ/DRP/ผลลัพธ์/ผู้รายงาน/ช่วงวันที่/ค้นหา)
- กดดูรายละเอียดเต็ม → **แก้ไขได้ทุกฟิลด์** + **เก็บประวัติเวอร์ชันก่อนแก้** + ป้าย ✎ แก้ไข
- Export CSV เฉพาะที่กรอง

**Dashboard**
- KPI (นับขึ้น count-up) · กราฟรายเดือน 6 เดือน · ระดับความรุนแรง
- แยกประเภท Med/DRP · ยาที่พบบ่อย · ผลลัพธ์ intervention
- แยกตาม OPD (ทั่วไป/NCD) · ช่วงเวร · ลักษณะความคลาดเคลื่อน
- Near miss (A–B) % · นับ High-alert / LASA
- เลือกช่วงเวลา (ทั้งหมด/เดือนนี้/ไตรมาส/ปีนี้/กำหนดเอง) · Export CSV · 🖨 พิมพ์สรุป (PDF)

**ตั้งค่า** — ใส่ Supabase URL + key เอง (เก็บใน localStorage) หรือปล่อยว่างเพื่อรันโหมด demo
_(v0.9.1: กำลังจะปรับหน้านี้ — ซ่อน config ฐานข้อมูลสำหรับผู้ใช้จริง + เพิ่มเกี่ยวกับแอป/จัดการรายชื่อ · ดู `docs/ROADMAP.md`)_

**📱 รองรับมือถือ (v0.9.1)** — หัวเมนู / หน้ารายการ / Dashboard ปรับเป็นการ์ด + แนวตั้งบนจอมือถืออัตโนมัติ (เดสก์ท็อปคงเดิม)

**🆕 v0.9.2.0**
- **Dropdown ผู้รายงานทำเอง** (ไม่ใช้ `<select>` ของ OS) — ชื่อยาวอยู่บรรทัดเดียว ไม่ตก 2 บรรทัดบน iOS
- **Heatmap เวร × วันในสัปดาห์** ในหน้า Dashboard — ดูว่าเวร/วันไหนเกิดเหตุบ่อย
- แก้บั๊กเส้นขอบช่องกรอก/เมนูหายหลังกด (โฟกัสใช้ `border` เต็มแทน `border-color`)
- มือถือ: วันที่เกิดเหตุจัดชิดซ้าย + ป้าย "วันนี้" ไม่ลอยตกขอบ (เดสก์ท็อปไม่แตะ)

**🆕 v0.9.2.1**
- **Dropdown ผู้รายงานเด้งขึ้น/ลงอัตโนมัติ** — ช่องอยู่ล่างจอ เมนูเด้งขึ้นบน (ไม่โดนตัดขอบล่าง)
- **แท่งกราฟ Dashboard** เป็น gradient teal + โชว์ %
- **สลับแท็บเด้งขึ้นบนสุดเสมอ** (ไม่ค้างตำแหน่งเลื่อนจากหน้าก่อน)
- **เดโม่เหลือ 10 เคส + ใช้รายชื่อผู้รายงานจริง 16 คน** (ตัวกรองไม่มีชื่อเก่า)

**🆕 v0.9.3.0**
- **เพิ่มประเภท Error "Pre-dispensing" (จัดยา)** แยกจาก Dispensing (จ่ายยา) → 5 ประเภท
- **ปุ่มประเภท Error โชว์ English (ไทย)** เช่น Pre-dispensing (จัดยา) · มือถือจัดเป็นตาราง 2 คอลัมน์
- **ประเภท Error เลือกได้หลายอัน** (เก็บเป็น array/jsonb) — ต้องรัน `supabase/migrations/0001_error_type_to_jsonb.sql` ที่ Supabase ก่อน deploy

**🆕 v0.9.4.0**
- **แก้บั๊กบันทึกไม่เข้า Supabase** — ฟอร์มส่ง `""` ให้ `severity`/`outcome` (มี CHECK constraint) → ถูกตีกลับ 400 · แก้ที่ `toRow` แปลง `""` → `null` ก่อนส่ง
- **ระบบบังคับกรอกช่องจำเป็น** — ดาวแดง `*` + ข้อความเตือนสีแดง · บันทึกไม่ได้ถ้ากรอกไม่ครบ (ร่วม: HN · วันที่ · จุดที่พบ · ผู้รายงาน · Med: ประเภท Error · ลักษณะฯ · ความรุนแรง · รายละเอียดเหตุการณ์ · DRP: ประเภท DRP · สาเหตุ · Intervention · ผลลัพธ์ · รายละเอียด)
- **จุดที่พบมีทั้ง Med และ DRP** + เปลี่ยนชื่อเป็น "ห้องยา OPD ทั่วไป / ห้องยา OPD NCD / ห้องยา IPD"
- **เลือก "ห้องยา IPD" → กรอก AN (เลขที่ผู้ป่วยใน)** รูปแบบ `69-01234` · เก็บคอลัมน์ใหม่ `an` — ต้องรัน `supabase/migrations/0002_add_an_column.sql`
- **จัดเรียงหน้ากรอกใหม่** — จุดที่พบ → HN + AN แถวเดียว (AN จาง/กดไม่ได้จนกว่าจะเลือก IPD)
- **ปุ่ม ⓘ ดูความหมาย** ที่ช่อง "ลักษณะความคลาดเคลื่อน" (กางคำอธิบายทุกอัน เหมือน A–I)

**🆕 v0.9.5.0**
- **หน้า "เกี่ยวกับ"** (แทนหน้าตั้งค่าเดิม) — ข้อมูลแอป · ผู้พัฒนา (เลขใบประกอบ + สังกัด + อีเมลกดได้) · เก็บข้อมูลที่ Supabase (โลโก้) · เทคโนโลยี Next.js/React/TypeScript/Supabase/Cloudflare + Claude Code · หมายเหตุ PDPA · **เอาช่องกรอก Supabase key ออก** (ตั้งผ่าน env แล้ว)
- **เมนูใหม่:** เดิม "ตั้งค่า" → เปลี่ยนเป็น **"เกี่ยวกับ"** + เพิ่มปุ่ม **"ตั้งค่า"** แยก (หน้าใหม่ · ตอนนี้ยังเป็นหัวข้อ placeholder "เร็ว ๆ นี้")
- **ใส่ชื่อ "รพ.ปรางค์กู่"** ในหัวเว็บ (มือถือ+เดสก์ท็อป) + ชื่อแท็บเบราว์เซอร์

**🆕 v0.9.6.0**
- **คลังยา 416 ตัว + ระบบค้นหา (autocomplete)** — ตาราง `drugs` ใน Supabase (ยา รพ.ปรางค์กู่) โหลดครั้งเดียว cache ในเครื่อง → พิมพ์ในช่อง "ชื่อยา" แล้ว suggest เด้งทันที (ค้นได้ทั้งชื่อ generic + ชื่อการค้า)
- **suggest แสดง:** ชื่อ (ไฮไลต์คำค้น) · ความแรง (+% · สูตรผสมใส่วงเล็บ) · รูปแบบ · route (IV/IM/oral) · ธง **Preg เรียงสีตามความเสี่ยง (A เขียว → X แดง)** / HAD / Renal
- เลือกยา → ได้ข้อความบรรทัดเดียวครบ (snapshot — แก้คลังยาไม่กระทบเคสเก่า) · พิมพ์เองได้ถ้าไม่เจอในระบบ
- SQL: `supabase/migrations/0003_create_drugs.sql` + `scripts/drugs_seed.sql` (416 ยา)

**🆕 v0.9.7.0**
- **ข้อมูลสดข้ามเครื่อง (Realtime)** — เครื่องหนึ่งบันทึก/แก้เคส อีกเครื่องเห็นทันทีโดยไม่ต้องรีเฟรช (สัญญาณหลุดแล้วกลับมาที่แท็บ = ดึงข้อมูลใหม่ให้อัตโนมัติ) · SQL: `supabase/migrations/0004_enable_realtime.sql`
- **Med Error — ติ๊ก "แก้ไขเรียบร้อยแล้ว"** ได้เลยโดยไม่ต้องพิมพ์บรรยาย (จะพิมพ์เพิ่มก็ได้) · หน้ารายละเอียดขึ้นป้ายเขียว "✓ แก้ไขแล้ว" · SQL: `0005_add_managed.sql`
- **DRP ยกเครื่องตามตำรา** — ประเภทปัญหาเป็น **10 หมวดคลาสสิก (Hepler & Strand)** เพิ่ม "มีข้อบ่งใช้แต่ไม่ได้รับยา" / "ได้ยาโดยไม่มีข้อบ่งใช้" และแยกขนาดยาสูง-ต่ำ · ทุกหมวดมีวงเล็บอังกฤษกำกับ + ปุ่ม **ⓘ ดูความหมาย** กางคำอธิบายครบ (เหมือนฝั่ง ME) · เคสเก่าไม่กระทบ (ค่าที่บันทึกคงเดิม)
- **DRP — ยุบช่องซ้ำซ้อน** "สาเหตุของปัญหา" + "รายละเอียดเพิ่มเติม" เหลือช่องเดียว "รายละเอียดเหตุการณ์ / สาเหตุ"
- **DRP — ติ๊ก "เภสัชกรแก้ไขเอง ไม่ผ่านแพทย์"** → ซ่อนช่องผลตอบรับจากแพทย์ (ไม่บังคับกรอก) · SQL: `0006_add_pharmacist_only.sql`
- **DRP — ป้ายชัดขึ้น** "การ Intervention" → "การแก้ไข (Intervention)" (ไม่ค้างค่าเริ่มต้นอีกต่อไป) · "ผลลัพธ์การ Intervention" → "ผลตอบรับจากแพทย์"
- **ปุ่มแนบรูปปิดใช้งานชั่วคราว** (ป้าย "ยังใช้ไม่ได้") — รอย้ายไปเก็บรูปที่ Cloudflare แบบแปลง webp + ย่อขนาด แทนการฝังรูปลงฐานข้อมูล

**🆕 v0.9.7.1** (ปรับหน้า DRP ตามการใช้งานจริง)
- **ผลตอบรับจากแพทย์โผล่เมื่อจำเป็น** — กรอบนี้จะขึ้นเฉพาะตอนเลือกการแก้ไขเป็น "ปรึกษาแพทย์ผู้สั่งใช้" (เลือกอย่างอื่น = ไม่ได้เสนอแพทย์ ก็ไม่ต้องกรอก) · เปลี่ยนใจไปเลือกอย่างอื่น ระบบล้างผลตอบรับที่ค้างให้อัตโนมัติ
- **ช่อง "การแก้ไข / จัดการ" ใน DRP** (จะพิมพ์เพิ่มหรือไม่ก็ได้ · อยู่ล่างสุด) — เขียนขยายความว่าจัดการอย่างไรต่อ เหมือนฝั่ง ME
- **ปุ่มติ๊ก "เภสัชกรแก้ไขเอง ไม่ผ่านแพทย์"** ย้ายขึ้นไปอยู่เหนือช่องการแก้ไข + เปลี่ยนเป็น **สีอำพัน**

**🆕 v0.9.7.2**
- **หน้าต่างรายละเอียดไม่ปิดเองอีกต่อไป** — คลิกพื้นที่ว่างนอกหน้าต่างไม่ปิด (ต้องกดปุ่มปิดเอง) · ถ้ากำลังแก้ไขค้างอยู่แล้วกดปิด จะถามยืนยัน "ปิดโดยไม่บันทึกการแก้ไข" ก่อนเสมอ (กันงานที่ยังไม่บันทึกหายไป)
- **กราฟแยกตามผู้รายงาน** ในหน้า Dashboard — เรียงมากไปน้อย สีสดคนละสีต่อคน
- **เปลี่ยนคำ "รายการ" → "รายงาน"** ทั้งเมนูและหัวข้อหน้า
- **เพิ่มยา Sofosbuvir + Velpatasvir (400 + 100 mg)** เข้าคลังยา (`scripts/add_drug_sof_vel.sql`)

**🆕 v0.9.7.3**
- **แก้บั๊ก cross-browser (Safari กรอกแล้วข้อมูลไม่ขึ้นระบบ)** — ต้นตอ: ตัวสร้างรหัส `id` เดิม ถ้าเบราว์เซอร์ไม่มี `crypto.randomUUID` (Safari รุ่นเก่า / เปิดผ่านลิงก์ที่ไม่ใช่ https / เว็บวิวในแอป) จะได้ค่าที่ไม่ใช่ UUID → Supabase ตีกลับ (คอลัมน์ `id` เป็นชนิด uuid) แล้วโค้ดดัก error เงียบ ๆ ขึ้น "บันทึกแล้ว" หลอก → เครื่องอื่นไม่เห็นข้อมูล · แก้ด้วยตัวสร้าง UUID v4 ที่ทำงานได้ทุกเบราว์เซอร์ (`lib/helpers.ts` → `uuid()`)
- **ระบบยืนยันการส่งจริง** — บันทึกแล้วเช็คผลก่อนแจ้ง (มี timeout 12 วิ กันค้าง) · สำเร็จ = "บันทึกและส่งขึ้นระบบเรียบร้อย ✓" · ไม่สำเร็จ = เก็บลงเครื่อง + **แถบเตือนสีเหลืองด้านบน** บอกจำนวนที่ค้าง + แนะนำให้เปิดผ่าน Google Chrome
- **คิวส่งซ้ำอัตโนมัติ + กู้รายงานเก่า** — รายงานที่ส่งไม่สำเร็จเข้าคิวในเครื่อง (`meddrp_pending_v1`) แล้วลองส่งใหม่เองตอนเปิดแอป / เน็ตกลับมา / สลับกลับมาที่แอป (ออก uuid ใหม่ให้ของเก่าที่รหัสผิด) · โหลดข้อมูลเปลี่ยนจาก "ทับทิ้ง" เป็น "รวม" — ไม่ลบรายงานที่ยังไม่ขึ้นระบบทิ้ง
- **ลบรายงาน 2 ชั้น (ถังขยะ)** — 🗑 ปุ่มลบในหน้ารายละเอียด (สีจาง กันเผลอกด) → **ชั้น 1 ลบแบบซ่อน** ย้ายไปถังขยะ (กู้คืนได้) → ถังขยะอยู่ในหน้า **ตั้งค่า** เลือก **กู้คืน** หรือ **ชั้น 2 ลบถาวร** (ต้องพิมพ์ HN ของเคสให้ตรงก่อนถึงกดได้ · กู้คืนไม่ได้) · รายงานในถังขยะไม่นับใน Dashboard · ต้องรัน `supabase/migrations/0007_soft_delete.sql` (คอลัมน์ `deleted_at` + RLS policy delete)

**🆕 v0.9.8.0**
- **DRP เลือกระดับความรุนแรง A–I ได้** (NCC MERP · บังคับกรอก) — เพราะ ME เป็นซับเซ็ตของ DRP ควรให้คะแนนความรุนแรงได้เหมือนกัน · ใช้ช่องเดียวกับ Med Error (แยกเป็นฟังก์ชันร่วม `renderSeverityField()` ในฟอร์ม/โหมดแก้ไข/รายละเอียด/ประวัติ)
- **เลือกยา HAD จากคลังยา → ติดธง High-alert อัตโนมัติ** — `pickDrug()` เช็ค `d.had` แล้ว `setField("high_alert", true)` ให้เอง (ผู้ใช้ปลดเองได้ · ไม่ auto-off)
- **Dashboard กราฟความรุนแรง A–I นับรวม DRP ด้วย** — เดิม `bySev` กรองเฉพาะ `type === "med"` · ตอนนี้นับทุกเคสที่มี severity (ป้าย "ระดับ E ขึ้นไป" ใต้การ์ด Med Error ยังนับเฉพาะ ME ตามความหมายของการ์ด)

**🆕 v0.9.8.1**
- **Safari กดส่งครั้งเดียวไปเลย** — จาก log: POST ที่ถึง server = 201 ทุกอัน (ไม่มี error) → "ครั้งแรกไม่ไป" เกิดที่ฝั่งเบราว์เซอร์ (คำขอแรกสะดุด: cold start / ITP / เน็ตวืบ) · แก้ `pushIncident` ให้ **ลองส่งซ้ำเองในกดครั้งเดียว** (3 รอบ · AbortController ยกเลิกคำขอค้างก่อนลองใหม่ · id เดิม = idempotent, 23505 = สำเร็จ)
- **หน้าผลการส่งเต็มจอ** (แทน toast) — สำเร็จ = "ส่งสำเร็จ" (✓ เขียว) + ปุ่มส่งใหม่/ดูรายงาน · ไม่สำเร็จ = "ส่งไม่สำเร็จ" (✕ แดง ตัวโต ไม่หายเอง) + **คำแนะนำเปิดผ่าน Google Chrome + โลโก้ Chrome** + ปุ่ม "ส่งอีกครั้ง" (idempotent) / "เก็บไว้ส่งทีหลัง"
- **เสียง + สั่นเตือน (ตอนส่งไม่สำเร็จ)** — 🔊 เสียงบี๊บ (Web Audio สังเคราะห์เอง · ปลดล็อกตอนกดปุ่ม รองรับ iOS) · 📳 `navigator.vibrate` เฉพาะ Android (iOS/Safari ไม่รองรับ)
- **หน้ารายงาน DRP โชว์ "จุดที่พบ"** — เดิม `recRows.place` gate เฉพาะ med → DRP โชว์ "—" · แก้เป็น `r.location || "—"` (จุดที่พบเป็นช่องร่วม ME+DRP)

**🆕 v0.9.12.1 — จัดบ้านโค้ด (Phase 1 · ไม่เปลี่ยนหน้าตา/การใช้งาน)**
- แยกไฟล์ยักษ์ `MedDrpApp.tsx` (5,202 บรรทัด) ออกเป็นส่วน ๆ ให้แก้ง่าย/ปลอดภัยขึ้น — ย้ายสไตล์ปุ่มไป `lib/styles.ts` · ฟังก์ชันจัดการรายงานไป `lib/records.ts` · ชนิดข้อมูล state ไป `MedDrpApp.types.ts` · **เป็นการย้ายที่อยู่โค้ดล้วน ๆ พฤติกรรมเว็บเหมือนเดิมทุกอย่าง**

**🆕 v0.9.12.0 — "ประเภท Error" เข้าหน้า DRP + เวรตามเวลาจริง + ลักษณะใหม่**
- **ช่อง "ประเภท Error" มีในหน้า DRP แล้ว** (เดิมมีเฉพาะ Med) — วางเป็นช่องแรกของ DRP เหมือนหน้า ME · เลือกได้หลายอัน (สั่งยา / คัดลอก / จัดยา / จ่ายยา / บริหารยา) · **บังคับกรอกทั้ง Med และ DRP** · มีทั้งหน้ากรอกและตอนกดแก้ไขเคส DRP
- **Dashboard การ์ด "แยกตามประเภท Error" นับรวม Med + DRP** (เดิมนับเฉพาะ Med) — ดูภาพรวมได้ว่าความคลาดเคลื่อนเกิดที่ขั้นตอนไหนมากสุด รวมทุกประเภท
- **ไม่ต้องแตะฐานข้อมูล** — คอลัมน์ `error_type` ใช้ร่วมกันอยู่แล้ว ไม่มี SQL ใหม่สำหรับส่วนนี้
- **แก้บั๊กเวรไม่ย้ายเองเมื่อเปิดเว็บค้างทั้งวัน** — เดิมพอถึง 16:00 ระบบไม่ย้ายจากเวรเช้าเป็นเวรบ่ายจนกว่าจะรีเฟรช · ตอนนี้เวรขยับตามเวลาจริงให้อัตโนมัติ (จนกว่าจะกดเลือกเวรเอง = เคารพที่เลือก)
- **ลักษณะความคลาดเคลื่อน:** เปลี่ยน "ค้างยา" → **"ยาขาด/ค้างยา"** (ยาไม่พอ/ของหมด ได้ไม่ครบ) + เพิ่ม **"ลืมจัด/จ่ายยา"** (ลืมทั้งรายการ) · ⚠️ รัน `scripts/rename_kangya_error_nature.sql` เปลี่ยนชื่อลักษณะในเคสเก่า "ค้างยา" → "ยาขาด/ค้างยา"

**🆕 v0.9.11.1 — ล็อก scroll ทุก popup + ลักษณะ "ค้างยา"**
- **เปิด popup ทุกตัวแล้วพื้นหลังไม่เลื่อนตาม** (เดิมล็อกแค่ป๊อปคลังยา · ขยายให้ครอบคลุมหน้าต่างรายละเอียด/แก้ไขเคส · หน้าผลการส่ง · ป๊อปลบถาวร · ป๊อปสลับ ME↔DRP)
- **เพิ่มลักษณะความคลาดเคลื่อน "ค้างยา"** (มียาค้างจ่าย ผู้ป่วยยังไม่ได้รับยาครบตามสั่ง) — เลือกได้ในฟอร์ม/โหมดแก้ไข · โชว์ในหน้า ⓘ ดูความหมาย · นับใน Dashboard · ค้นหาได้

**🆕 v0.9.11.0 — ช่อง "หน่วยงานต้นเหตุ" + กราฟใน Dashboard**
- ช่องใหม่ **"หน่วยงานต้นเหตุ"** บันทึกว่าความคลาดเคลื่อน**มาจากวิชาชีพ/หน่วยไหน** (แพทย์ · ทันตแพทย์ · พยาบาล · เภสัชกร · ผู้ช่วยเภสัช · ห้องแล็บ · เวชระเบียน · ผู้ป่วย / ญาติ · อื่น ๆ)
- แยกจาก "ประเภท Error" ที่เป็น**ขั้นตอน** (เพราะวิชาชีพไม่ตรงกับขั้นตอน 1:1 · เช่น พยาบาลบางทีคีย์คำสั่ง · IPD เภสัชเป็นคนคัดลอกคำสั่ง)
- **เลือกได้หลายอัน · บังคับกรอก** · ใช้ทั้ง Med + DRP · วางถัดจาก "จุดที่พบ" (ทั้งหน้ากรอก + โหมดแก้ไข) · เลือก "อื่น ๆ" ต้องพิมพ์ระบุ
- **Dashboard: กราฟใหม่ "แยกตามหน่วยงานต้นเหตุ"** (1 เคสนับได้หลายหน่วย · รวม Med + DRP) · โชว์ในหน้ารายละเอียด + ค้นหา + ส่งออก CSV ได้
- ⚠️ ต้องรัน migration `0013_source_units` (applied แล้ว) · เคสเก่าที่ไม่มีค่าโชว์ "—" · เข้าไปแก้เคสเก่าจะต้องเลือกหน่วยงานต้นเหตุด้วย

**🆕 v0.9.10.3 — โหมดแก้ไขใหม่ (เหมือนหน้ากรอก + เลือกยาจากคลัง)**
- กด "แก้ไขรายการ" → หน้าตาเหมือนหน้ากรอก (แบบมินิมอล) · ประเภท/ลักษณะเป็นชิปสี · **ช่องยาเลือกจากคลังได้** (พิมพ์ค้น → autocomplete)
- เลือกยาจากคลังในโหมดแก้ไข = **ผูกรหัสยา (ID) ให้เคส** → ใช้ปรับ ID เคสเก่าที่จับคู่อัตโนมัติไม่ได้ (ยาที่เคยเปลี่ยนชื่อ)

**🆕 v0.9.10.2 — ผูกรหัสยาเข้ากับเคส (เปลี่ยนชื่อยาแล้วตามทั้งหมด)**
- เวลาเลือกยาจากคลัง เคสจะ **จำรหัสยา (ID)** ไว้ · เปลี่ยนชื่อยาในคลังทีเดียว **ทุกเคส + Dashboard เปลี่ยนตามทันที** ไม่แตกเป็นยาชื่อคล้ายกัน 2 ตัว
- **Dashboard "ยาที่พบบ่อย" นับรวมยาตัวเดียวกันถูกต้อง** แม้เคยกรอกด้วยชื่อเก่า
- **เคสเก่าจับคู่รหัสยาให้อัตโนมัติ** (ยาที่ยังไม่เปลี่ยนชื่อ) · ที่เหลือปรับเองได้ (Phase ถัดไป)
- ยาที่พิมพ์เอง (ไม่ได้เลือกจากคลัง) ยังแสดงตามข้อความเดิม
- ⚠️ ต้องรัน migration `0012_incidents_drug_ids` (applied แล้ว)

**🆕 v0.9.10.1 — ปรับหน้าคลังยาให้ใช้ง่ายขึ้น**
- แถบค้นหา + ตัวกรอง **ตรึงไว้ด้านบน** (เลื่อนดูรายการยาวก็ยังค้นได้) · ปุ่มลอย **↑ / ↓** ไปบนสุด/ล่างสุด
- **หัวคอลัมน์กดเรียงได้** (สีเข้ม · กดสลับน้อย↔มาก) · เพิ่มคอลัมน์ **ID** · ย้าย **ชื่อการค้า** มาไว้ต้น ๆ
- **ตัวกรองเลือกได้หลายอัน** (กดซ้ำ = ยกเลิก) + ปุ่ม **ล้างตัวกรอง**
- ป๊อปแก้ไข: **หน่วย / รูปแบบ / ทางให้ยา เป็นเมนู dropdown** (เลือกจากที่มีอยู่ กันพิมพ์ไม่ตรงกัน) · เปิดป๊อปแล้วเพจด้านหลังไม่เลื่อน

**🆕 v0.9.10.0 — หน้า "คลังยา" (จัดการรายการยาในระบบ)**
- **หน้าคลังยาใหม่** (เมนู "คลังยา") — ค้นหา / กรอง (HAD · รูปแบบ · Preg) / **เพิ่ม / แก้ไข** ยา → เข้า Supabase ทันที + ทุกเครื่องเห็นสด · มีปุ่ม **ส่งออก CSV**
- **ID อัตโนมัติ** — เพิ่มยาใหม่ระบบออกเลขให้เอง ไม่ต้องกรอก · เลข ID ไม่เปลี่ยนแม้แก้ = เป็นยาเดิมเสมอ
- **ประวัติการแก้ไข (audit log)** — เก็บทุกการเพิ่ม/แก้/เปลี่ยนชื่อยาไว้ในระบบ (แม้แก้ตรงใน Supabase ก็จับได้) · กดปุ่ม "ประวัติ" ดูไทม์ไลน์ + การเปลี่ยนชื่อ (เก่า→ใหม่) ได้
- **ลบไม่ได้จากเว็บ → "ซ่อน" แทน** — กดซ่อนยาที่ไม่ใช้ (หายจากการค้นหา/ตอนกรอก) · เอากลับมาแสดงได้ที่หน้าตั้งค่า · ลบจริงทำได้เฉพาะใน Supabase · การซ่อน/เอากลับก็เก็บเป็น log
- **ป๊อปแก้ไขกันปิดพลาด** — กดที่ว่างไม่ปิด + แก้ค้างแล้วกดปิด = เตือนก่อน
- ⚠️ ต้องรัน migrations `0009_drugs_writable` · `0010_drug_audit` · `0011_drugs_hidden` (applied แล้ว)

**🆕 v0.9.9.1**
- **คลังยา sync สดข้ามเครื่อง** — แอดมินเพิ่ม/แก้ยาในระบบ ทุกเครื่องที่เปิดแอปเห็นทันทีไม่ต้องรีเฟรช (realtime ตาราง `drugs` แบบเดียวกับรายงาน · ต้องรัน `supabase/migrations/0008_enable_drugs_realtime.sql`)
- **กราฟรายเดือน 6 → 12 เดือน + ปุ่มเลือกปี** — โชว์ ม.ค.–ธ.ค. ของปีที่เลือก (พ.ศ.) · หัวข้อบอกปี · กดปุ่มปีดูย้อนหลังได้
- **หน้ารายงานเพิ่มคอลัมน์ "รายละเอียด"** — โชว์ข้อความเหตุการณ์ที่พิมพ์ไว้ (เดสก์ท็อป = คอลัมน์ใหม่ ตัด 2 บรรทัด · มือถือ = บรรทัดล่างการ์ด)
- **แก้ปุ่ม "ส่งรายงานใหม่"** — กดแล้วเด้งขึ้นบนสุดของหน้ากรอก (เดิมค้างอยู่ล่างเพจ)

**🆕 v0.9.9.0 — ยกเครื่องตามผลตรวจโค้ดเชิงลึก (แก้ 24 จุด)**
- **🔴 กันข้อมูลหาย/ผิด** — กันกดปุ่มบันทึกซ้ำ → เคสซ้ำ · หน้าแก้ไขเลือก "ประเภท/ลักษณะ" ได้หลายอันแล้ว (เดิมเหลืออันเดียว) · ลบรายงานที่ยังไม่ขึ้นระบบ = เอาออกจากคิวด้วย (ไม่เด้งกลับ) · หน้าแก้ไขยืนยันผลจริง + ลองส่งซ้ำ (เดิมขึ้น "บันทึกแล้ว" หลอก) · หน้าแก้ไขตรวจช่องบังคับ · คิว/แคช race-safe (อ่าน-แก้-เขียนทีละรายการ)
- **🟠 สถิติให้ตรง** — การ์ด "แพทย์รับข้อเสนอ %" หารเฉพาะเคสที่เสนอแพทย์จริง · ตารางรายงานล่าสุดโชว์หลายประเภทถูกต้อง · กราฟ "6 เดือน" ไม่หดตามตัวกรองช่วงเวลา · ปุ่ม CSV ใน Dashboard ตามตัวกรอง · สลับ ME↔DRP ตอนกรอกค้าง = ป๊อปยืนยันก่อน
- **🟡 เก็บกวาดขอบเคส** — **AN พิมพ์ `1234` → `69-01234` อัตโนมัติ** (ปีตรวจเองจากปีที่เกิดเหตุ · เติมศูนย์ครบ 5 หลัก ตอนย้ายช่อง) · เปลี่ยนจุดที่พบออกจาก IPD ล้าง AN ให้ · ยา HAD ปลดธง High-alert ให้เองเมื่อลบยา (แต่ไม่ยุ่งธงที่ติ๊กเอง) · "อื่น ๆ" บังคับพิมพ์ระบุ · ร่างอัตโนมัติเก็บครบขึ้น · ค้นหาเฉพาะช่องที่เห็น (ไม่เจอมั่ว) · CSV กัน formula injection · ถังขยะสดข้ามเครื่อง + ปุ่มโชว์สถานะทำงาน + ป๊อปลบถาวรโชว์ตัวตนเคส

---

## 🚀 เริ่มใช้งาน (local dev)

```bash
npm install
cp .env.example .env.local   # ใส่ค่า Supabase (มีค่า default ให้แล้วใน .env.local)
npm run dev                  # http://localhost:3000
```

### ตัวแปรสภาพแวดล้อม (`.env.local`)
```
NEXT_PUBLIC_SUPABASE_URL=https://ryewggkhunpuipgkgbfv.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_...
```
> ค่าเหล่านี้เป็นคีย์ฝั่ง client (เปิดเผยได้) การเข้าถึงจริงถูกคุมด้วย RLS ของ Supabase
> ผู้ใช้ยังตั้งค่า URL/key เองได้ที่หน้า **ตั้งค่า** หรือปล่อยว่างเพื่อรัน **โหมด demo** (localStorage + เดโม 100 เคส)

---

## 🗄️ ฐานข้อมูล Supabase

- โปรเจกต์: **tb-calculator** (`ryewggkhunpuipgkgbfv`, region ap-northeast-1)
- ตาราง: **`public.incidents`** (เก็บทั้ง Med Error และ DRP แยกด้วยคอลัมน์ `type`)
- Schema เต็ม + RLS อยู่ที่ [`supabase/schema.sql`](supabase/schema.sql)
- มีข้อมูลเดโม 100 เคส (ติดธง `is_demo = true` — ลบได้ด้วย `delete from public.incidents where is_demo;`)

พฤติกรรมการโหลดข้อมูล (`lib/data.ts` + `components/MedDrpApp.tsx`):
1. โชว์ข้อมูลในเครื่อง (cache/seed) **ทันที** ไม่บล็อกด้วย network
2. ถ้าตั้งค่า Supabase แล้ว → ดึงข้อมูลจริงมาทับเบื้องหลัง

---

## ☁️ Deploy ขึ้น Cloudflare Workers

```bash
npm run cf:preview   # build + รันจำลอง Worker ในเครื่อง
npm run cf:deploy    # build + deploy ขึ้น Cloudflare (ต้อง wrangler login ก่อน)
```

ตั้งค่า env บน Cloudflare (อย่างใดอย่างหนึ่ง):
- **Dashboard:** Workers & Pages → โปรเจกต์ → Settings → Variables → เพิ่ม
  `NEXT_PUBLIC_SUPABASE_URL` และ `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- **CLI:** `npx wrangler secret put NEXT_PUBLIC_SUPABASE_URL` (และ key)

> `NEXT_PUBLIC_*` ถูกฝังตอน build — ต้องมีค่าตอนรัน `cf:deploy` (ผ่าน `.env.local` หรือ env ของ CI)

ตั้งค่า Worker อยู่ที่ [`wrangler.jsonc`](wrangler.jsonc) และ [`open-next.config.ts`](open-next.config.ts)

---

## 📁 โครงสร้างโปรเจกต์

```
app/                     # Next.js App Router (layout, page, globals.css)
components/
  MedDrpApp.tsx          # แอปหลักทั้งหมด (state + ทุกหน้า + modal) — พอร์ตจากดีไซน์ DC
  ui.tsx                 # คอมโพเนนต์ hover/focus (HButton/HInput/HSelect/...)
lib/
  constants.ts           # ค่าคงที่ (ERROR_TYPES, SEVERITY, DRP_TYPES, ...)
  types.ts               # TypeScript types
  helpers.ts             # today/nowTime/shiftOf/format/nature-drug helpers
  data.ts                # Supabase client + fetch/insert/update
  seed.ts                # เดโม 100 เคส (โหมด demo)
  style.ts               # แปลง CSS string → React style (คงดีไซน์เดิมแบบ pixel-perfect)
supabase/schema.sql      # โครงตาราง + RLS
project/                 # 🎨 ดีไซน์ต้นฉบับจาก Claude Design (อ้างอิง)
chats/                   # 💬 บทสนทนาการออกแบบ (อ้างอิง)
docs/                    # บันทึก session
```

---

## 🔒 หมายเหตุความปลอดภัย (สำคัญก่อนใช้จริง)

- ตอนนี้ RLS เปิดให้ **anon** อ่าน/เขียนได้โดยไม่ต้องล็อกอิน (เพื่อความเร็วในการทดสอบ)
- ก่อนใช้จริงกับข้อมูลผู้ป่วย แนะนำ: ทำ **Supabase Auth** + จำกัดสิทธิ์ (เภสัชกรเห็น/แก้เฉพาะของตน, หัวหน้าเห็นทั้งหมด),
  พิจารณาการเก็บ **HN แบบเข้ารหัส/ปิดบางส่วน** ให้สอดคล้อง **PDPA** และนโยบายโรงพยาบาล

---

## 🧭 สิ่งที่ทำต่อได้ (roadmap)

- คลังชื่อยา autocomplete (ลดพิมพ์ผิด + ทำให้ "ยาที่พบบ่อย" แม่นขึ้น)
- Supabase Auth + สิทธิ์ผู้ใช้ + แจ้งเตือนเคสระดับ E–I / High-alert
- สรุปรายเดือน PDF อัตโนมัติ · ค่าคงที่ (dropdown) แก้ได้เองโดยไม่ต้องแก้โค้ด
- แนบรูปขึ้น Supabase Storage แทน data URL
