# SESSION 2026-07-19f — v0.9.12.5

จัดบ้านโค้ด **Phase 3 Step 1–5** — ยก handler อิสระเป็น custom hooks (ชุดปลอดภัย)

---

## สิ่งที่ทำ (push แล้ว → main · commit `277bbbd`)

**pure move (ไม่เปลี่ยนพฤติกรรม/ตรรกะ)** · build เขียว + verify runtime จริง (Playwright)
`MedDrpApp.tsx`: 1,851 → **1,604 บรรทัด** · รวมทั้งโปรเจกต์ **5,202 → 1,604 (−69%)**

### hooks ใหม่ (`components/hooks/`)
- **`core.ts`** — `type Core = { setState, stateRef }` (ใช้ร่วม)
- **`useAudioAlert.ts`** (Step 1) — `unlockAudio`/`alertFail` (+ `audioCtxRef` ภายใน) · ไม่แตะ state
- **`useDraft.ts`** (Step 2) — `saveDraft`/`clearDraft`/`draftSoon`/`hasDraftContent` (+ `dtRef`)
- **`useToast.ts`** (Step 3) — `flash` (+ `tRef`)
- **`useDrugsAdmin.ts`** (Step 4) — CRUD คลังยา + ป๊อปแก้ยา + ประวัติ + กรอง/เรียง/CSV (14 handler) ·
  รับ `core` + `flash`
- **`useDashboard.ts`** (Step 5) — `computeKpiTargets`/`animateKpi`/`animateKpis`/`setDashPreset`
  (+ `ivRef`)

### ประเด็นสำคัญ
- แต่ละ hook **จัดการ timer/cleanup ของตัวเอง** (`useEffect` cleanup) → ย้าย `dtRef`/`tRef`/`ivRef`
  ออกจาก mount effect ของ MedDrpApp (mount cleanup ว่างแล้ว)
- MedDrpApp เรียก hook ที่ต้นคอมโพเนนต์ (หลัง `setState`) แล้ว spread ผลเข้า `ctx` เหมือนเดิม
- state ยังก้อนเดียว (setState merge เดิม)
- แก้ import ระหว่างทาง: เอา `dashRecs` import ที่ไม่ใช้แล้วออกจาก MedDrpApp (ย้ายไป useDashboard)

## Verify (Playwright · inject เคสจริง)
- form โหลดปกติ (audio/draft/toast wired OK) · คลังยา + ป๊อปเพิ่มยา (useDrugsAdmin) ·
  Dashboard KPI "เคสทั้งหมด" animate เป็น 1 จากเคสที่ inject (**useDashboard `animateKpis` ทำงานจริง**)
- ไม่มี app error (WebSocket Supabase ใน sandbox เท่านั้น)

## ค้างไว้ (Phase 3 Step 6–9 · อนาคต · เสี่ยงสูง)
- `useFormMutations` — setField/toggle/pickDrug/switchType
- `useEditForm` — setEf/efArr/efToggleArr/startEdit ฯลฯ
- `useRecords` — **ก้อนใหญ่สุด** loadRecords/refreshRecords/คิวส่ง/save/saveEdit/resendResult/
  delete/exportCsv (พันกัน · ทำท้าย)
- `useRealtime` + mount effect
- **แนะนำ:** ทำ Step 6–9 เป็นเซสชันใหม่สด ๆ · เสี่ยงกว่า (closure/ref/effect-deps ของ save/คิว)
