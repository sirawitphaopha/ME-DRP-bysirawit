# SESSION 2026-07-19d — v0.9.12.3

จัดบ้านโค้ด **Phase 2c + 2d** (refactor · แยก Dashboard + Records)

---

## สิ่งที่ทำ (push แล้ว → main · commit `f621e0a`)

**pure move (ไม่เปลี่ยนพฤติกรรม/หน้าตา)** · build เขียว + verify runtime จริง (Playwright)

### Phase 2c — Dashboard
- `components/views/dashData.ts` — ยกบล็อกคำนวณค่าสรุปทั้งหมด (~280 บรรทัด) เป็น pure
  function **`computeDashData(S)`** (kpis/กราฟรายเดือน/severity/breakdowns/heatmap/recent) +
  ย้าย `dashRecs`/`dashBounds` มาด้วย · **return type อนุมานเอง** ไม่ต้อง type มือ
- `components/views/DashboardView.tsx` — เรียก `computeDashData(S)` + `renderBarList` local ·
  ได้ orgName/animateKpi/exportCsv/setDashPreset จาก Context
- MedDrpApp: ลบ dashboard derived + renderDashboard/renderBarList · import `dashRecs` กลับ
  (computeKpiTargets ยังใช้)

### Phase 2d — Records
- `components/views/recordsData.ts` — **`computeRecordsData(S)`** = กรอง `rlist` + สร้าง `recRows`
- `components/views/RecordsView.tsx` — ตัวกรอง + ตาราง/การ์ด · opts คำนวณ local จาก constants
  (drpType/severity/outcome ที่ **EditMode ใช้ร่วม** ยังเก็บใน MedDrpApp)
- MedDrpApp: ลบ records derived + renderRecords/renderFilterField/renderFilterSelect

### ขนาดไฟล์หลัก
`MedDrpApp.tsx`: 4,317 → **3,326** (Phase 2c −701, Phase 2d −290) · รวมจากเริ่มต้น
5,202 → 3,326 (**−1,876 · ~36%**)

## Verify
- build เขียว typecheck strict · แก้บั๊ก import ระหว่างทาง 2 จุด (AppState/Drug import
  ผิดโมดูล — AppState อยู่ `components/MedDrpApp.types`, Drug อยู่ `lib/types`)
- Playwright screenshot: DashboardView (การ์ด/กราฟ/heatmap ครบ) · RecordsView (ตัวกรอง 9
  ช่อง + ตาราง + empty state) → เหมือนเดิมเป๊ะ · ไม่มี error เรื่อง Context

## ค้างไว้ (Phase 2 · เฟสสุดท้าย)
- **2e** — DetailModal + EditMode (หน้ารายละเอียด + โหมดแก้ไขเคส)
- **2f** — FormView (หน้ากรอก · คู่ state มากสุด เสี่ยงสุด ทำท้ายสุด)
- แผน: ทำ 2e+2f ให้จบเฟส 2 แล้ว push ปิดจ็อบทีเดียว (verify runtime แต่ละเฟส)
