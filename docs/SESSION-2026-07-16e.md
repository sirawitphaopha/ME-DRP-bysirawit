# SESSION 2026-07-16 (รอบ 5) — v0.9.10.3

**Phase 3 (จบแผนคลังยา)** — รื้อโหมดแก้ไขให้เหมือนหน้ากรอก (มินิมอล) + ช่องยาเป็น picker ผูกรหัส
**push ขึ้น `main`** · ไม่ต้องมี migration

---

## ที่ทำ
- **refactor `renderDrugRows` → `drugPickerUI(cfg)`** — ช่องเลือกยา autocomplete ใช้ร่วมได้ทั้งหน้ากรอก+โหมดแก้ไข
  (cfg: rows/sug/onSug/onBlur/onChangeAt/onPick/onRemove/onAdd) · `renderDrugRows` กลายเป็น wrapper บาง ๆ
- **โหมดแก้ไข: ช่องยา text → picker** — state `efDrugSug` · handlers `setEfDrugAt`/`pickEfDrug`/`addEfDrug`/`removeEfDrug`
  (parallel form · ใช้ `alignIds` · pickEfDrug ผูก drug_ids + ติดธง HAD)
- `startEdit` normalize `ef.drugs` (จาก det.drugs หรือ split det.drug) + `ef.drug_ids` (align)
- `saveEdit` สร้าง drugs/drug_ids จาก picker ที่ผูกไว้ตรง ๆ (เลิก re-match จากข้อความแบบ Phase 2)
- **ผล:** แก้เคสเก่า → เลือกยาจากคลังได้ → ผูก ID → ปรับ ID เคสที่ backfill อัตโนมัติไม่ได้ (ยาที่เคยเปลี่ยนชื่อ)

## เทส
- `tsc --noEmit` exit 0 · `npm run build` ผ่าน
- แคปหน้าจอจริง: โหมดแก้ไขเหมือนฟอร์ม (ปุ่มเวร/ชิปประเภท-ลักษณะ/ช่องยา picker) + พิมพ์ "War" → autocomplete Warfarin จากคลัง

## ไฟล์ที่แตะ
- `components/MedDrpApp.tsx` — drugPickerUI + wrapper · efDrugSug + edit drug handlers · startEdit/saveEdit
- `lib/constants.ts` — APP_VERSION 0.9.10.2 → 0.9.10.3
- `CLAUDE.md` / `README.md`

## commit รอบนี้
- `d829d51` Phase 3 (drugPickerUI + edit picker + ID)
- `<bump>` v0.9.10.3 + docs

## แผนคลังยา — จบครบ 3 เฟส 🎉
- P1 (v0.9.10.0–.1): หน้าคลังยา · CRUD/audit log/ซ่อน/ปรับ UI
- P2 (v0.9.10.2): ผูกรหัส ID ยา · Dashboard ไม่นับซ้ำ · backfill อัตโนมัติ
- P3 (v0.9.10.3): โหมดแก้ไขใหม่ (picker+ID)
- migrations: 0009 (writable) · 0010 (audit) · 0011 (hidden) · 0012 (drug_ids) — applied ครบ
